targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-prometheus"
pkgver="2.41.0"
pkgrel="7"
pkgdesc="An open-source systems monitoring and alerting toolkit"
pkgdesclong=(
  "An open-source systems monitoring and alerting toolkit"
)
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
section="mail"
priority="optional"
arch="amd64"
sources=(
  "https://github.com/prometheus/prometheus/releases/download/v${pkgver}/prometheus-${pkgver}.linux-amd64.tar.gz"
  "prometheus.default"
  "prometheus.service"
  "prometheus.sysusers"
  "prometheus.yml"
  "carbonio-prometheus.sh"
  "policies.json"
)
hashsums=(
  "026f3fe429853cff4266c7ab4f771342"
  "9d1820a430e80092bacf83d0d9969fcc"
  "ce889e2b65786c3bd66416b488fabcd7"
  "aa87ed719c2714f4a26f74ebae3ab79a"
  "255e3c0df9462b1fbbf677da271ff672"
  "52a38d4a95e93c7be26a915e45750542"
  "c2c5933682af87720cdc0f9a90a0bc63"
)

depends=(
  "systemd"
)

conflicts=(
  "prometheus"
)

 backup=(
   "/etc/carbonio/${pkgname}/prometheus.yml"
)


package() {
  cd "${srcdir}/prometheus-${pkgver}.linux-amd64"
  
  install -Dm 755 prometheus "${pkgdir}/usr/bin/prometheus"
  install -Dm 755 promtool "${pkgdir}/usr/bin/promtool"

  install -dm 750 -o210 -g210 "${pkgdir}/var/lib/prometheus"

  install -Dm 640 -g210 "${srcdir}/prometheus.yml" "${pkgdir}/etc/carbonio/${pkgname}/prometheus.yml"
  install -Dm 640 -g210 "${srcdir}/policies.json" "${pkgdir}/etc/carbonio/${pkgname}/service-discover/policies.json"
  cp -r consoles* "${pkgdir}/etc/carbonio/${pkgname}/"
	
  install -Dm 644 "${srcdir}/${pkgname}.sh" "${pkgdir}/etc/zextras/pending-setups.d/${pkgname}.sh"
  
  install -Dm 644 "${srcdir}/prometheus.service" "${pkgdir}/usr/lib/systemd/system/${pkgname}.service"
  install -Dm 644 "${srcdir}/prometheus.sysusers" "${pkgdir}/usr/lib/sysusers.d/${pkgname}.conf"
  install -Dm 644 "${srcdir}/prometheus.default" "${pkgdir}/etc/default/${pkgname}"
}

postinst() {
  if hash initctl 2>/dev/null; then
    initctl reload-configuration 2>/dev/null || true
  fi

  systemd-sysusers >/dev/null || true
  systemd-tmpfiles --create >/dev/null || true

  if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || :
    systemctl enable ${pkgname}.service >/dev/null 2>&1 || :
  fi

  echo "======================================================"
  echo "Carbonio Prometheus installed successfully!"
  echo "You have to complete config running pending setup"
  echo "======================================================"
}

prerm() {
  if [ -d /run/systemd/system ]; then
    systemctl --no-reload disable ${pkgname} >/dev/null 2>&1 || :
    systemctl stop ${pkgname} >/dev/null 2>&1 || :
  fi
}

postrm() {
  if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || :
  fi
}