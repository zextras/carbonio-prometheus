groups:
  - name: imap_service_down
    interval: 15s
    rules:
      - alert: imap_service_down_1h
        expr: probe_success{job="blackbox-imap"} == 0
        for: 1h
        labels:
          severity: blocker
          service: imap
          job: blackbox-imap
        annotations:
          summary: "IMAP service is unreachable on {{ $labels.instance }}"
          description: "Blackbox probe to IMAP (port 143) failed for {{ $labels.instance }} for more than 1 hour. Check the mailbox service, firewall, and network connectivity."
      - alert: imap_service_down_30m
        expr: probe_success{job="blackbox-imap"} == 0
        for: 30m
        labels:
          severity: warning
          service: imap
          job: blackbox-imap
        annotations:
          summary: "IMAP service is unreachable on {{ $labels.instance }}"
          description: "Blackbox probe to IMAP (port 143) failed for {{ $labels.instance }} for more than 30 minutes."
      - alert: imap_service_down_1m
        expr: probe_success{job="blackbox-imap"} == 0
        for: 1m
        labels:
          severity: alert
          service: imap
          job: blackbox-imap
        annotations:
          summary: "IMAP service is unreachable on {{ $labels.instance }}"
          description: "Blackbox probe to IMAP (port 143) failed for {{ $labels.instance }} for more than 1 minute."
